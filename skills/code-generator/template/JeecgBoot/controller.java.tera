$!callback.setFileName($tool.append($tableInfo.name, "Controller.java"))
$!callback.setSavePath($tool.append($tableInfo.savePath, "/controller"))

import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import lombok.extern.slf4j.Slf4j;
import org.jeecg.common.api.vo.Result;
import org.jeecg.common.aspect.annotation.AutoLog;
import org.jeecg.common.aspect.annotation.PermissionData;
import org.jeecg.common.constant.CommonConstant;
import org.jeecg.common.system.base.controller.JeecgController;
import org.jeecg.common.system.query.QueryGenerator;
{% if tableInfo.savePackageName %}
import {{ tableInfo.savePackageName }}.entity.{{ tableInfo.name }};
import {{ tableInfo.savePackageName }}.service.I{{ tableInfo.name }}Service;
{% endif %}
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.servlet.ModelAndView;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.util.Arrays;

/**
 * @Description: {{ tableInfo.comment }}控制器
 * @Author: {{ author }}
 * @Date: {{ currentTime }}
 */
@Slf4j
@Tag(name = "{{ tableInfo.comment }}")
@RestController
@RequestMapping("{{ tableInfo.nameLowerFirst }}")
public class {{ tableInfo.name }}Controller extends JeecgController<{{ tableInfo.name }}, I{{ tableInfo.name }}Service> {
    @Autowired
    private I{{ tableInfo.name }}Service {{ tableInfo.nameLowerFirst }}Service;

    /**
     * 分页列表查询
     *
     * @param {{ tableInfo.nameLowerFirst }}
     * @param pageNo
     * @param pageSize
     * @param req
     * @return
     */
    @Operation(summary = "{{ tableInfo.comment }}-分页列表查询")
    @GetMapping(value = "/list")
    @PermissionData(pageComponent = "{{ tableInfo.nameLowerFirst }}/{{ tableInfo.name }}List")
    public Result<?> queryPageList({{ tableInfo.name }} {{ tableInfo.nameLowerFirst }},
                                   @RequestParam(name = "pageNo", defaultValue = "1") Integer pageNo,
                                   @RequestParam(name = "pageSize", defaultValue = "10") Integer pageSize,
                                   HttpServletRequest req) {
        QueryWrapper<{{ tableInfo.name }}> queryWrapper = QueryGenerator.initQueryWrapper({{ tableInfo.nameLowerFirst }}, req.getParameterMap());
        Page<{{ tableInfo.name }}> page = new Page<{{ tableInfo.name }}>(pageNo, pageSize);
        IPage<{{ tableInfo.name }}> pageList = {{ tableInfo.nameLowerFirst }}Service.page(page, queryWrapper);
        return Result.OK(pageList);
    }

    /**
     * 添加
     *
     * @param {{ tableInfo.nameLowerFirst }}
     * @return
     */
    @AutoLog(value = "{{ tableInfo.comment }}-添加")
    @Operation(summary = "{{ tableInfo.comment }}-添加")
    @PostMapping(value = "/add")
    public Result<?> add(@RequestBody {{ tableInfo.name }} {{ tableInfo.nameLowerFirst }}) {
        {{ tableInfo.nameLowerFirst }}Service.save({{ tableInfo.nameLowerFirst }});
        return Result.OK("添加成功！");
    }

    /**
     * 编辑
     *
     * @param {{ tableInfo.nameLowerFirst }}
     * @return
     */
    @AutoLog(value = "{{ tableInfo.comment }}-编辑", operateType = CommonConstant.OPERATE_TYPE_3)
    @Operation(summary = "{{ tableInfo.comment }}-编辑")
    @RequestMapping(value = "/edit", method = {RequestMethod.PUT, RequestMethod.POST})
    public Result<?> edit(@RequestBody {{ tableInfo.name }} {{ tableInfo.nameLowerFirst }}) {
        {{ tableInfo.nameLowerFirst }}Service.updateById({{ tableInfo.nameLowerFirst }});
        return Result.OK("编辑成功！");
    }

    /**
     * 通过id删除
     *
     * @param id
     * @return
     */
    @AutoLog(value = "{{ tableInfo.comment }}-通过id删除")
    @Operation(summary = "{{ tableInfo.comment }}-通过id删除")
    @DeleteMapping(value = "/delete")
    public Result<?> delete(@RequestParam(name = "id", required = true) String id) {
        {{ tableInfo.nameLowerFirst }}Service.removeById(id);
        return Result.OK("删除成功!");
    }

    /**
     * 批量删除
     *
     * @param ids
     * @return
     */
    @AutoLog(value = "{{ tableInfo.comment }}-批量删除")
    @Operation(summary = "{{ tableInfo.comment }}-批量删除")
    @DeleteMapping(value = "/deleteBatch")
    public Result<?> deleteBatch(@RequestParam(name = "ids", required = true) String ids) {
        this.{{ tableInfo.nameLowerFirst }}Service.removeByIds(Arrays.asList(ids.split(",")));
        return Result.OK("批量删除成功！");
    }

    /**
     * 通过id查询
     *
     * @param id
     * @return
     */
    @Operation(summary = "{{ tableInfo.comment }}-通过id查询")
    @GetMapping(value = "/queryById")
    public Result<?> queryById(@RequestParam(name = "id", required = true) String id) {
        {{ tableInfo.name }} {{ tableInfo.nameLowerFirst }} = {{ tableInfo.nameLowerFirst }}Service.getById(id);
        if ({{ tableInfo.nameLowerFirst }} == null) {
            return Result.error("未找到对应数据");
        }
        return Result.OK({{ tableInfo.nameLowerFirst }});
    }

    /**
     * 导出excel
     *
     * @param request
     * @param {{ tableInfo.nameLowerFirst }}
     */
    @RequestMapping(value = "/exportXls")
    @PermissionData(pageComponent = "{{ tableInfo.nameLowerFirst }}/{{ tableInfo.name }}List")
    public ModelAndView exportXls(HttpServletRequest request, {{ tableInfo.name }} {{ tableInfo.nameLowerFirst }}) {
        return super.exportXls(request, {{ tableInfo.nameLowerFirst }}, {{ tableInfo.name }}.class, "{{ tableInfo.comment }}");
    }

    /**
     * 通过excel导入数据
     *
     * @param request
     * @param response
     * @return
     */
    @RequestMapping(value = "/importExcel", method = RequestMethod.POST)
    public Result<?> importExcel(HttpServletRequest request, HttpServletResponse response) {
        return super.importExcel(request, response, {{ tableInfo.name }}.class);
    }

}